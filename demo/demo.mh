@import "prelude"

let a = (x) => c + 1
let c = 22

print("Hello, world!")

let fibonaci: Int -> Int = (n) =>
  match n then
  | n if n <= 2 => 1
  | n => fibonaci(n - 1) + fibonaci(n - 2)

let start = get_timestamp()
print(fibonaci(5))
print(get_timestamp() - start)
